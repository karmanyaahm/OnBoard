name: Gerbview Comment
run-name: Gerbview Comment
on: 
    pull_request: 
        types: [opened]
        paths: 'projects/**'

jobs:
  Requisite-Check:
    name: Check for files
    runs-on: ubuntu-latest
    steps:
        - name: Check out
          uses: actions/checkout@v2

        - name: Testing
          run: TOKEN="${{ secrets.GITHUB_TOKEN }}" node .github/index.js